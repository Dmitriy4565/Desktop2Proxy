<h1 align="center">Desktop2Proxy</h1>

<p align="center">
  <b>Многофункциональный сетевой сканер и помощник по безопасности на Go</b><br>
  Ваш «швейцарский нож» для IP- и порт-сканирования, проверки доступности хостов и анализа сети.
</p>

<p align="center">
  <img src="https://img.shields.io/badge/lang-Go-blue?logo=go" alt="Golang">
  <img src="https://img.shields.io/badge/platform-Windows%20%7C%20Linux%20%7C%20macOS-success" alt="Platforms">
  <img src="https://img.shields.io/badge/status-Active%20Development-orange" alt="Status">
</p>

---

## 🚀 Ключевые возможности

| Модуль / Функция        | Описание                                                                 |
|-------------------------|--------------------------------------------------------------------------|
| Сканер IP-адресов       | Поиск активных хостов в сети, одиночный IP, диапазоны и подсети.       |
| Сканер портов           | Определение открытых TCP-портов на целевых хостах.                     |
| Проверка доступности    | Быстрый ping / TCP-проверки для понимания, «жив» ли хост.              |
| Гибкая конфигурация     | Таймауты, количество потоков, диапазоны портов, режимы сканирования.   |
| Удобный вывод           | Читаемый CLI-вывод, форматированный результат для последующего анализа.|

---

## 📦 Установка

### Требования

- Go 1.21+ (или актуальная стабильная версия)
- Поддерживаемые ОС: Windows, Linux, macOS

### Сборка из исходников

```bash
git clone https://github.com/Dmitriy4565/Desktop2Proxy.git
cd Desktop2Proxy
go build -o desktop2proxy ./cmd/desktop2proxy
```

После сборки бинарный файл `desktop2proxy` (или `desktop2proxy.exe` на Windows) можно запускать напрямую.

---

## 🖥️ Быстрый старт

### Скан одного IP

```bash
./desktop2proxy scan --ip 192.168.0.10
```

### Скан диапазона IP и портов

```bash
./desktop2proxy scan --ip 192.168.0.1-192.168.0.254 --ports 1-1024
```

### Скан подсети

```bash
./desktop2proxy scan --ip 10.0.0.0/24 --ports 80,443
```

### Настройка таймаутов и потоков

```bash
./desktop2proxy scan --ip 10.0.0.0/24 --ports 1-65535 --timeout 2s --workers 200
```

### Справка по командам

```bash
./desktop2proxy --help
./desktop2proxy scan --help
```

---

## 🧩 Структура проекта

```text
Desktop2Proxy/
├─ cmd/
│  └─ desktop2proxy/      # Точка входа CLI
├─ internal/
│  ├─ scanner/            # Логика сканирования IP/портов
│  ├─ netutil/            # Вспомогательные сетевые функции
│  └─ output/             # Форматирование и вывод результатов
└─ README.md              # Этот файл
```

Такое разделение позволяет легко добавлять новые режимы (баннер‑граббинг, HTTP‑проверки, работу через прокси и т.д.).

---

## 🛣️ Дорожная карта

Планы/идеи для развития проекта:

- Добавить вывод результатов в JSON/CSV
- Реализовать сохранение отчётов в файл
- Добавить работу через HTTP/SOCKS5‑прокси
- Веб‑панель для визуализации и запуска сканов
- Интеграция с системами мониторинга и SIEM

Если у тебя есть идеи — создай issue или Pull Request.

---

## 🤝 Как внести вклад

1. Форкни репозиторий:
   ```bash
   git clone https://github.com/Dmitriy4565/Desktop2Proxy.git
   ```
2. Создай ветку под свою фичу или багфикс:
   ```bash
   git checkout -b feature/my-awesome-feature
   ```
3. Внеси изменения и сделай коммит:
   ```bash
   git commit -am "Add my awesome feature"
   ```
4. Отправь ветку в свой форк:
   ```bash
   git push origin feature/my-awesome-feature
   ```
5. Создай Pull Request в основной репозиторий.

---

## 📜 Лицензия

Укажи здесь тип лицензии (например, MIT/Apache‑2.0) и добавь файл `LICENSE` в корень проекта.

---

<p align="center">Made with ❤️ for cybersecurity community</p>
